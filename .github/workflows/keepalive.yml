name: Keep Replit Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Replit
        run: |
          curl -s -o /dev/null -w "Status: %{http_code}\n" \
            "https://ed974cae-9d46-4e56-9dc7-786491142d49-00-145b9fbg1y1e1.worf.replit.dev"
